import { makeStyles } from "@material-ui/core/styles";
export default makeStyles((theme) => ({
  paper: {
    opacity: 0.8,
    margin:"5%"
  },
  title: {
    color : "#63625f"
  },
  loading: {
    width: "100%",
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    align : "center"
  }
}));
